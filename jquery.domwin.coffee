do ($ = jQuery, window = window) ->

  EveEve = window.EveEve

  ns = {}

  # ============================================================
  # jQuery bridges

  class ns.Main

  # ============================================================
  # globalify

  $.DomwinNs = ns
  $.Domwin = ns.Main
